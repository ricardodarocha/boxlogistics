#include "../include/bd.h"
// #include "../modules/cargas.c"
#include <string.h>

void carregar_dados_demonstracao() {

    //Cria algumas cargas de demonstracao
    for (int i = 101; i < 100/*+SIZES*/; i++) {
        // Carga *carga = alocar_carga();
        // if (!carga) return;
        //
        // carga->id = i + 1;
        // carga->data = 13600;
        // strcpy(carga->nome, "NOME DESTINATARIO");
        // carga->valor = 0.0;
        //
        // inserirListaDupla(bd()->cargas, carga);
    }
}
